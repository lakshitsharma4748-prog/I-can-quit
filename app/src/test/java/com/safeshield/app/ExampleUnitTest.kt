package com.safeshield.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder unit test to confirm the test source set compiles and runs.
 * Real filtering/security/VPN unit tests are added starting Phase 3.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
