<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. Actual color scheme is applied by the Compose
         Material 3 theme in ui/theme/Theme.kt; this XML theme exists so the
         Activity has a valid theme reference before Compose content is set. -->
    <style name="Theme.SafeShield" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
